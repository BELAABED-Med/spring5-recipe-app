spring:
  datasource:
    url: jdbc:mysql://localhost:3306/beles_prod
    username: beles_prod_user
    password: beles
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false

